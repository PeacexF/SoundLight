{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the SoundLight window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
